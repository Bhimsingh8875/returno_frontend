import 'package:flutter/cupertino.dart';
import 'package:returno/navigation_services/navigator_key.dart';

var h = MediaQuery.of(Navigation_services.navigatorkey.currentContext!).size.height;
var w = MediaQuery.of(Navigation_services.navigatorkey.currentContext!).size.width;
